# Core
flask==3.0.0
markdown==3.5.1
python-dateutil==2.8.2
python-dotenv==1.0.0

# Authentication (optional - only if FIREBASE_CONFIG is set)
firebase-admin==6.2.0

# Google Cloud Storage (for GCS upload pipeline)
google-cloud-storage==2.14.0
urllib3<2.0  # Fix OpenSSL compatibility warning with Python 3.9

# Production server
gunicorn==21.2.0
